{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aiops.fullname" . }}-secrets
  labels:
    {{- include "aiops.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
